export const enum PAGES_ROUTES {
  login = '/',
  register = '/register',
  chat = '/chat',
  profile = '/profile',
  profileChangeData = '/profile-change-data',
  profileChangePassword = '/profile-change-password',
  notFound = '/not-found',
  serverError = '/server-error',
}
